import { LazyTabContentDirective } from './lazy-tab-content.directive';

describe('LazyTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
